import { Routes } from '@angular/router';
import { EventComponent } from './event.component';
export default [
    {
        path     : 'event',
        component: EventComponent,
    },
] as Routes;
